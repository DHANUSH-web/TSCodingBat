# bun-dev

To install dependencies:

```bash
bun install
```

To run the migrated module:

```bash
bun run start
```

To run the test suite:

```bash
bun test
```

This project now contains the TypeScript logic migrated from `deno-test`, with Deno-specific test/runtime APIs replaced by Bun equivalents.
